Frontend scaffold for CryptoTracker.

Open files in a browser (e.g., open `frontend/index.html`).

Notes:
- JS expects backend REST endpoints like `/api/coins` and auth routes.
- To run integrated with backend, host `frontend` static files from Spring Boot `resources/static` or serve separately and configure CORS.
